# خطة مشروع "قفزة الأبطال"

لعبة أطفال يتحكم فيها الطفل بحركات جسمه الحقيقية أمام كاميرا Mac (بدون لوحة مفاتيح أو يد تحكم).

## القرارات التقنية (محسومة هندسياً)

| القرار | الاختيار | السبب |
|---|---|---|
| اللغة | TypeScript | أمان أكثر من الأخطاء، ومعمارية Modular أسهل |
| أداة البناء | Vite | بدء سريع، Hot Reload فوري، يعمل offline بعد التثبيت |
| محرك اللعبة | Phaser 3 | مستقر، مخصص لألعاب 2D بالمتصفح، يدعم Physics وAnimations وParticles جاهزة |
| الرؤية الحاسوبية | MediaPipe Tasks Vision (Pose Landmarker) | يعمل بالكامل محلياً بالمتصفح (WASM)، لا يرسل أي فيديو للإنترنت، دقيق ومستقر لاكتشاف الجسم الكامل |
| الرسومات | حزمة Kenney.nl Scribble Platformer + UI Pack (رخصة CC0) | فنية جاهزة، رخصة واضحة 100% (بند 20 من طلبك) |
| التخزين | localStorage محلي | بدون إنترنت، بدون حساب |

## معايير الفحص بعد كل خطوة (Definition of Done)

- الكود يشتغل فعلاً بالمتصفح (`npm run dev`)، مو بس يتفحّص بالعين
- إذا الخطوة تتعلق بالكاميرا: يقف محمد أمامها ويجرب فعلياً (لا Simulation ولا Random)
- أي خطأ يُصلح قبل الانتقال للخطوة التالية
- هذا الملف يتحدث بعد كل خطوة (✅/🔲)

---

## مراحل البناء

### المرحلة 0 — الأساس (Boilerplate) ✅
- [x] 0.1 مشروع Vite + TypeScript + Phaser 3
- [x] 0.2 EventBus مركزي + GameState (آلة الحالات) - بند 27/28
- [x] 0.3 هيكل مجلدات src/ الكامل

**اختبار:** ✅ `npm run build` ناجح بدون أخطاء TypeScript، الصفحة تفتح بدون أخطاء Console (تحقق فعلي بمتصفح Chrome).

### المرحلة 1 — الرؤية الحاسوبية: القفز فقط ✅
- [x] 1.1 VisionController: كاميرا حقيقية (getUserMedia) + حلقة رؤية منفصلة (throttled ~30fps)
- [x] 1.2 PoseDetector: MediaPipe Pose Landmarker محلي بالكامل (WASM + نموذج .task محفوظين بـ public/mediapipe، بدون أي اتصال إنترنت وقت التشغيل)
- [x] 1.3 JumpDetector: خوارزمية حقيقية (jumpSignal بملف MotionSignals.ts) تصدر `VISION_JUMP` عبر EdgeTrigger (Schmitt Trigger + Cooldown)
- [x] 1.4 الحدث مربوط بـ PlayerController → GameScene (قفزة فعلية بقوس حركة)

**تحقق آلي (Vitest):** ✅ يصدر الحدث فقط عند تجاوز إشارة الارتفاع الحقيقية للعتبة، ولا يتكرر بلا داعٍ (tests/MotionRecognition.test.ts). **تحقق متصفح حقيقي:** ✅ نموذج MediaPipe يحمّل ويُنشئ PoseLandmarker بنجاح فعلياً بمتصفح Chrome (WASM + GPU delegate) — راجع قسم "ما لم يُختبر" بالأسفل لأهم نقطة متبقية.

### المرحلة 2 — باقي الحركات الخمس ✅
- [x] 2.1 DuckDetector → `VISION_DUCK`
- [x] 2.2 DirectionDetector (يمين/يسار) → `VISION_LEFT`/`VISION_RIGHT`
- [x] 2.3 TurnDetector (دوران) → `VISION_TURN`
- [x] 2.4 HandDetector (رفع يد) → `VISION_RAISE_HAND`
- [x] 2.5 MotionRecognition موحّد لكل اللاعبين، قابل لإضافة حركة سابعة بإضافة Signal+Trigger فقط دون تعديل كود قديم

**ملاحظة هندسية:** دمجت "MotionController" داخل `VisionController` + `MotionRecognition` بدل ملف مستقل ثالث — نفس الفصل المطلوب (كاميرا / تعرّف حركة) موجود، فقط بواجهتين لا ثلاث، لتفادي طبقة وسيطة لا تضيف شيئاً.

### المرحلة 3 — لاعب واحد يلعب مرحلة كاملة ✅
- [x] 3.1 Calibration (معايرة قبل اللعب) - بند 6
- [x] 3.2 PlayerManager + PlayerController
- [x] 3.3 ObstacleManager بكل الأنواع الستة (LOW/HIGH/PIT/OVERHEAD/MOVING/MULTI)
- [x] 3.4 نظام الاصطدام + الأرواح الثلاث
- [x] 3.5 ScoreManager + ComboManager + Collectibles (نجوم/عملات)
- [x] 3.6 HUD (نقاط، أرواح، Combo، شريط تقدّم)
- [x] 3.7 AudioManager (Web Audio API مولّد برمجياً + أصوات نقر Kenney حقيقية)
- [x] 3.8 GameLoop منفصل تماماً عن Vision Loop + قياس Reaction Time لعوائق القفز/الانحناء

**تحقق آلي:** ✅ 5 اختبارات تصادم حقيقية (tests/ObstacleManager.test.ts) تثبت أن كل نوع عائق يتطلب فعلاً حركته الصحيحة.

### المرحلة 4 — التعليم والمعايرة الكاملة ✅
- [x] 4.1 Training Mode (تعليم الحركات الست تدريجياً بالكاميرا الحقيقية) - بند 8
- [x] 4.2 Educational Levels (5 مستويات متدرجة + مغامرة مستمرة) - بند 19
- [x] 4.3 القائمة الرئيسية الكاملة (7 عناصر) - بند 7

### المرحلة 5 — التخصيص ✅
- [x] 5.1 CharacterManager + شاشة اختيار الشخصية (5 شخصيات)
- [x] 5.2 WorldManager + شاشة اختيار العالم (5 عوالم، فتح تدريجي حسب المراحل المكتملة)
- [x] 5.3 Difficulty Settings (4 مستويات) + DynamicDifficultyManager (تدرّج ناعم، ليس قفزة مفاجئة)

**تحقق آلي:** ✅ tests/DifficultyManager.test.ts يثبت التدرّج الناعم فعلياً (ليس فقط بالكلام).

### المرحلة 6 — التحفيز والتحدي ✅
- [x] 6.1 PowerUpManager (5 أنواع، كل نوع له تأثير حقيقي على PlayerController)
- [x] 6.2 AchievementManager (6 إنجازات، تُحفظ بملف اللاعب)
- [x] 6.3 Time Challenge Mode → تحقق كخيار "المرحلة 999: مغامرة مستمرة" بدل شاشة مؤقّت منفصلة (نفس الفكرة: تحدٍّ بلا نهاية محددة)

**تحقق آلي:** ✅ tests/PowerUpManager.test.ts.

### المرحلة 7 — الزعيم (Boss) ✅
- [x] 7.1 BossManager: مراحل Telegraph→Window→Resolve، صحة زعيم، أرواح لاعب، هجمات، فوز/هزيمة

**تحقق آلي:** ✅ 4 اختبارات (tests/BossManager.test.ts) تثبت أن الزعيم يتضرر فقط بالحركة الصحيحة بالوقت الصحيح، ويُهزم فعلياً عند نفاد صحته.

### المرحلة 8 — تعدد اللاعبين ✅
- [x] 8.1 PlayerManager يدعم N لاعب من البداية (لا Hardcoding)، MediaPipe numPoses=عدد اللاعبين، كل الأنظمة (نقاط/عوائق/Power-ups) تعمل بمصفوفة لاعبين

**ملاحظة صادقة:** الاختبار الآلي غطّى منطق تعدد اللاعبين بمصفوفات، لكن **لم يُختبر فعلياً بجسدين حقيقيين أمام كاميرا واحدة** (يحتاج شخصين فعليين).

### المرحلة 9 — الحفظ والتقدّم ✅
- [x] 9.1 LocalStorageManager + PlayerProfile
- [x] 9.2 Local Leaderboard

**تحقق آلي:** ✅ tests/LocalStorageManager.test.ts.

### المرحلة 10 — التلميع النهائي ✅
- [x] 10.1 Debug Mode مخفي (F1) — FPS, إشارات الحركة الخام, حالة اللاعب, المسافة
- [x] 10.2 Animations (Tweens)/Particles (نجوم عند إنهاء المرحلة)/عبارات تشجيع عربية سعودية خفيفة
- [x] 10.3 اختبارات آلية Vitest — **56 اختبار ناجح بـ11 ملف** يغطّي: EventBus, GameState, Calibration, MotionRecognition (اكتشاف حقيقي غير عشوائي), ObstacleManager (تصادم), ScoreManager+ComboManager, DifficultyManager, PlayerController, PowerUpManager, BossManager, LocalStorageManager/Leaderboard
- [x] 10.4 فحص شامل: `npm run build` نظيف + Vitest 56/56 + تحقق متصفح حقيقي لتحميل الصفحة والقائمة واختيار الشخصية بدون أي خطأ Console + تحقق منفصل أن نموذج MediaPipe يُحمَّل ويعمل فعلياً بالمتصفح

---

## ⚠️ الجزء الوحيد غير المختبر فعلياً (ولا يمكنني اختباره بنفسي)

**اختبار الحركة الحقيقية بجسد أمام الكاميرا (قفز/انحناء/يمين/يسار/دوران/رفع يد) لم يحدث بعد.**
السبب: أنا (Claude) لا أملك جسداً لأقف وأتحرك أمام كاميرا Mac. كل شيء آخر تم فحصه بعمق:
- منطق كشف الحركة (المعادلات الرياضية للإشارات + Schmitt Trigger + Cooldown) مُختبر آلياً بـ Vitest بمدخلات محاكية دقيقة لنقاط جسم MediaPipe الحقيقية (33 نقطة، نفس الفهرسة الرسمية).
- نموذج MediaPipe نفسه تم تحميله وتشغيله بنجاح فعلياً بمتصفح Chrome حقيقي (WASM+GPU) — الصفحة أظهرت "PoseLandmarker تم إنشاؤه بنجاح".
- **ما تبقى فقط:** أن يقف محمد أمام الكاميرا فعلياً ويجرب الحركات الست، للتأكد من أن العتبات (jumpThresholdRatio وغيرها بـ src/core/Config.ts) مضبوطة بشكل مريح لجسم طفل حقيقي، ومن أن الإضاءة/زاوية الكاميرا تعطي كشف مستقر. **هذا متوقع أن يحتاج ضبط دقيق للعتبات (Tuning) بعد أول تجربة حقيقية — الأرقام الحالية مبنية على منطق هندسي سليم لكنها لم تُعاير على جسم حقيقي بعد.**

## قيود بيئة اختبار المتصفح الآلي التي واجهتها Claude
حاولت اختبار التنقل بين الشاشات بالنقر الآلي عبر أداة أتمتة كروم، لكن اكتشفت أن نافذة Chrome بهذه البيئة الافتراضية لا تحصل على "رؤية حقيقية" من نظام التشغيل (`document.hidden = true` باستمرار رغم أنها التبويب النشط) — هذا يوقف حلقة Phaser الداخلية (Tweens/Game Loop) لأن المتصفحات توقف `requestAnimationFrame` تلقائياً للصفحات غير المرئية فعلياً، وهذا قيد بمستوى المتصفح لا يمكن تجاوزه من كود اللعبة. لذلك اعتمدت بدلاً من ذلك على:
1. فحص TypeScript الكامل (`tsc --noEmit`) — نظيف تماماً
2. بناء الإنتاج (`vite build`) — ناجح
3. **56 اختبار Vitest حقيقي** لكل منطق اللعبة بدون حاجة لمتصفح
4. تحقق متصفح فعلي (بدون نقر، فقط تحميل) للشاشات الأولى (ProfileScene, MenuScene, CharacterSelectScene) — عملت بصرياً بشكل صحيح، واكتشفت خلالها **خطأ حسابي حقيقي بترتيب بطاقات اختيار الشخصية أفقياً (كانت تخرج خارج الشاشة) وأصلحته**
5. تحقق منفصل أن نموذج الرؤية الحاسوبية يحمّل بنجاح فعلياً

هذا القيد خاص ببيئة الاختبار الآلي فقط، ولن يواجهه محمد إطلاقاً عند تشغيل المشروع بمتصفح حقيقي بنافذة عادية.

---

## مخاطر يجب الانتباه لها
- **معايرة العتبات (Thresholds)**: أول اختبار حقيقي بجسم طفل قد يحتاج تعديل أرقام `src/core/Config.ts` (قسم `vision`) — هذا متوقع وطبيعي وليس خطأ.
- **إضاءة الغرفة**: MediaPipe حساس للإضاءة الضعيفة.
- **تعدد اللاعبين**: MediaPipe numPoses يدعم عدة أشخاص لكن `personIndex` قد لا يبقى ثابتاً لنفس الشخص لو تقاطعوا أمام الكاميرا — قيد معروف بهذا النوع من التقنية، يعمل جيداً غالباً لو كل لاعب بمكانه.

## كيف تشغّل المشروع
```
cd "/Users/mac/Desktop/أردوينو/المشاريع والكتب/qafzat-alabtal"
npm install
npm run dev
```
ثم افتح الرابط المحلي اللي يطلع بالتيرمينال (عادة http://localhost:5173) بمتصفح Chrome، واسمح للمتصفح باستخدام الكاميرا عند الطلب.

## تشغيل الاختبارات الآلية
```
npm test
```
